import { Text, View } from 'react-native'

export default function CalendarView() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
      }}
    >
      <Text>Calendar</Text>
    </View>
  )
}
